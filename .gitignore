# 依賴套件
node_modules

# 建置輸出（部署由 `npm run deploy` 透過 gh-pages 分支處理，無需版控）
dist

# 環境變數
.env
.env.local
.env.production

# 自動生成的 SCSS source maps
**/*.css.map

# 編輯器 / 系統檔案
.DS_Store
Thumbs.db